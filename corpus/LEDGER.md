# ERASCORP Ledger

No performance or conformance claim leaves this repo — README included — unless it appears
here with the run that produced it (mission plan §7, the rusty_zstd refusal discipline).

Every entry records: date, commit, scenario/cells, arms (ours + baseline build/flags), seeds,
numbers, the **method line** (pinned? interleaved? CPU or wall? how many pairs? null-arm
floor? work counts equal?), and the verdict. Deterministic gates (byte identity, censuses,
counts) note that they are one-run-true; timing verdicts without a method line are void.

| date | commit | scenario/cells | arms | numbers | method line | verdict |
|---|---|---|---|---|---|---|
| 2026-08-28 | 7acb448 (M1 tree) | GF(2^8) + matrix conformance, exhaustive | rusty_erasure-core vs ISA-L **v2.32.1** golden tables (`corpus/golden/`, extracted from `ec_base.h` sha256 `b9b3d8e5…`, see PROVENANCE.md) | **65,536/65,536** products byte-identical (both the const-table `mul` AND the independent shift-XOR `mul_shift`); **256/256** inverses incl. `inv(0)=0`; generator powers match all 255 exp entries; `table_mul` over all 65,536 `(c,x)`; RS + Cauchy constructions byte-match reference transcriptions on 12 configs (up to k=247); Vandermonde safe region enforced both sides; Cauchy sampled-submatrix invertibility 7 configs × 200 subsets; fuzz (ASan): matrix_gen 4.13M execs, matrix_invert 0.50M execs, clean | deterministic counts — one run is the verdict; no timing taken | **PASS** — field polynomial pinned by proof. One defect found & fixed by the no-panic sweep: `k + p` usize overflow panic in `check_dims` (now `checked_add`) |

| 2026-08-28 | 7acb448 (M2 tree) | Encode/update/recover conformance — the first ERASCORP conformance run | rusty_erasure-core vs golden vectors generated by real ISA-L **v2.32.1** `_base` code (`tools/oracle/gen_vectors.c` + unmodified `ec_base.c`, gcc under WSL Ubuntu; `encode_vectors.bin` sha256 `c408ebdf…`, 77 cases = 11 configs × 7 lengths incl. len 1/31/33/4113 tails) | **77/77** cases: expanded `gftbls` byte-identical to `ec_init_tables_base` (incl. its 64-bit construction path); encode byte-identical to `ec_encode_data_base`; update-sequence ≡ one-shot (asserted independently in C against the reference AND in Rust, plus random-order permutations); recovery gated on ground truth: **1470/1470 exhaustive loss patterns** on (10,4) (every subset of size 1..=4 over 14 shards, sources + parity), plus sampled patterns across the (2,2)…(32,8) grid × 6 lengths; verify() detects corruption; len-0 stripes valid; roundtrip fuzz (ASan): **694,740 execs clean** (encode → lose ≤p shards → recover → assert ground truth) | deterministic counts — one run is the verdict; no timing taken | **PASS** — encode path conformant vs real ISA-L output. Instrument finding: the fuzzer's first run caught an infinite loop in the fuzz TARGET's own loss-selection walk (`(s*31+17)%n` fixed point, e.g. n=7 s=2) — harness fixed to a bounded Fisher-Yates, the pathological input replays in 0 ms. Selection loops must be provably finite |

| 2026-08-28 | a00d47c | M3 conformance additions + **scalar encode baseline** | ISA-L compat layer + their erasure_code_test.c ported through it (7/7 green: RS/Cauchy 9×5 @8192, 64 random Cauchy configs to m=127, buffer-end sweep, singular-retry decode-matrix flow); Miri 31 tests, zero UB/leaks; compat fuzz 403,501 execs clean (ASan). Baseline arm: `scalar_baseline` example via `tools/bench/scalar_baseline.ps1` | Scalar encode, source-bytes basis: **(10,4,64 KiB): best 0.477 GB/s, median 0.463** · (10,4,1 MiB): best 0.479, median 0.476 · (4,2,4 KiB): best 0.945, median 0.918 (halved per-byte parity work). 64 KiB ≈ 1 MiB rate ⇒ **not cache-bound at these sizes** (streaming access; corpus S4 sweep will rule formally at M4/M5). ≈1.9G table-lookups/s ≈ 0.52 ns/mul pinned | pinned core 2, High priority, **CPU time** (TotalProcessorTime), N=7 identical runs per cell, work identity asserted via checksum; runs 7–9.8 s (startup <0.5% of arm); single-arm baseline: run spread = null floor = 6.1% / 3.5% / 29.9% (cell 1 had one noisy run; minima agree — best-of-N quoted per codec-measurement §1) | **BASELINE RECORDED** — the number M4's SIMD campaign must move; no comparative claim made |

| 2026-08-28 | M4 tree (pre-commit) | **First ERASCORP perf run vs ISA-L** + SIMD conformance + reach census | OUR arm: `rerasure bench` (Windows native, pinned core 2, High, CPU-time) scalar vs `x86_64/avx2` (SSSE3/AVX2 PSHUFB nibble kernels, 4-row fused), ABBA lead alternated, 3 rounds/arm, work parity via identical parity checksums. ISA-L arm: full autotools+nasm v2.32.1 build on the WSL Ubuntu rig (same i7-14650HX silicon: AVX2+GFNI, no AVX-512), `perf_arm.c` calling `ec_encode_data_avx2` (matched ISA, `ec_init_tables_base` tables) and dispatched `ec_encode_data` (avx2_gfni), taskset-pinned, process-CPU-time, 3 runs/arm, same splitmix data, same source-bytes basis | Best-of-3, src GB/s — (4,2,4 KiB): ours **22.47** / scalar 0.638 (**35.2×**) / ISA-L avx2 26.53 (**0.85×**) / ISA-L gfni 51.55 (0.44×) · **(10,4,64 KiB) S2: ours 13.98** / scalar 0.445 (**31.4×**) / ISA-L avx2 14.63 (**0.96×**) / gfni 17.93 (0.78×) · (10,4,1 MiB): ours **11.95** / scalar 0.466 (25.6×) / avx2 12.78 (**0.94×**) / gfni 14.98 (0.80×). **Census: 100.00%** of shipping-path bytes (encode + recovery, 131.7 MB workload) through the AVX2 kernels, 0 scalar. Oracle gates: 5/5 `*_matches_scalar` (both ISA levels, 17 edge lengths, fusion-boundary row counts); ported ISA-L suite 7/7 through the dispatching compat path; roundtrip fuzz 332,429 execs clean post-rewire. **Conformance bonus: our parity checksums match the dispatched avx2_gfni full build byte-for-byte on all 3 cells** — closing the M2 `_base`-only provenance caveat | our arm pinned CPU-time (TotalProcessorTime); ISA-L arm CLOCK_PROCESS_CPUTIME_ID under WSL2 on the same silicon (cross-OS-layer caveat noted; compute-bound); work counts equal by construction, verified by checksums | **M4 BAR CLEARED**: exit test was S2 within **1.5×** of ISA-L AVX2 — measured **0.96× (parity)**. Remaining gap is GFNI only (0.78× vs dispatched) → the M5 brick. Instrument finding: the first matched-ISA arm fed dispatched `ec_init_tables` output (AFFINE format on GFNI silicon) to the nibble-consuming `ec_encode_data_avx2` — wrong parity at plausible speed, caught ONLY by the checksum column; arms must pair init and encode formats. Their-perf drift note: ISA-L's own `erasure_code_perf` swung 33→11 GB/s (stripe basis) across invocations on the same cell — all ledger numbers are best-of-N interleaved for this reason |

_Next: M5 — GFNI kernels (stable-intrinsics check first), redundancy-elimination pass, G2 campaign._
