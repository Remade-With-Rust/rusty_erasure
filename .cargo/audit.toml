# cargo-audit policy (use-protection-please H-09), kept in lockstep with the
# `[advisories].ignore` list in deny.toml. Both advisories reach the graph
# ONLY through the test-only dev-dependency `reed-solomon-erasure` (the
# klauspost-compat conformance oracle); neither is in any shipped artifact —
# `cargo tree -e normal` shows five workspace crates plus rusty_alloc.
# Review 2027-02-28, or immediately if either gains a shipped-tree path.
[advisories]
ignore = [
    "RUSTSEC-2024-0384", # instant unmaintained (dev-only oracle tree)
    "RUSTSEC-2026-0253", # lru 0.7 LruCache::pop unsound (dev-only oracle tree)
]
