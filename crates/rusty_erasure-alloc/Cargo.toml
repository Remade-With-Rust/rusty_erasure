[package]
name = "rusty_erasure-alloc"
description = "The rusty_alloc seam for rusty_erasure: one crate, one exact pin, so the global allocator is a deliberate choice made once in the deliverables rather than forced on every library consumer. MIT OR Apache-2.0."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
documentation = "https://docs.rs/rusty_erasure-alloc"
readme = "../../README.md"
homepage.workspace = true
rust-version.workspace = true
keywords = ["allocator", "memory", "rusty-alloc", "seam"]
categories = ["memory-management", "no-std"]

[lints]
workspace = true

[dependencies]
rusty_alloc-api = "=1.1.6"
